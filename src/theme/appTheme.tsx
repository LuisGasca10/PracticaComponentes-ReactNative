import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    globalMargin: {
        marginHorizontal: 20,
        // backgroundColor: 'red'
    },

    title: {
        fontSize: 25,
        fontWeight: 'bold',
        color: 'black'
    }
});